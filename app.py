# ──────────────────────────────────────────────────────────
# 檔案：app.py（首頁）
# 說明：多頁 App 入口與全域設定（只在這裡 set_page_config）
# ──────────────────────────────────────────────────────────
import streamlit as st


st.set_page_config(
page_title="投資分析首頁",
page_icon="📊",
layout="wide",
)


st.title("📊 投資分析首頁（多頁版）")


st.markdown(
"""
歡迎使用多頁版投資分析網站！請依照左側（或上方）導覽順序：


1. 前往 **📥 上傳檔案** 頁，將你的對帳單（CSV/Excel）上傳。
2. 接著到 **🚀 執行分析** 頁，按下按鈕執行你的主分析流程，瀏覽報表與下載 Excel。


若你已有現成的分析程式碼，請將其整合到 `run_full_analysis()`（位於 **🚀 執行分析** 頁）。
"""
)


with st.expander("目前狀態 / 偵錯小工具"):
has_df = "uploaded_df" in st.session_state and st.session_state["uploaded_df"] is not None
st.write("已上傳資料：", "✅ 有" if has_df else "❌ 無")
if has_df:
st.dataframe(st.session_state["uploaded_df"].head(20), use_container_width=True)
